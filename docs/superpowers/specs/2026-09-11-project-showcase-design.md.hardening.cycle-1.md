# Hardening report — /Users/calebbolden/Projects/consulting/calebbolden.com/docs/superpowers/specs/2026-09-11-project-showcase-design.md

Verdict: fix-first. 12 finding group(s) survived across 3 reviewer seat(s).
Model refutation was enabled, but at least one verifier failed or omitted a verdict; those findings remain unverified. Deterministic evidence lookup still ran.
Reviewers: lens:DESIGN, lens:SCOPE, lens:SECURITY.
SINGLE-ENGINE WARNING: Independent review seat failed (transport); the legacy codex_note compatibility field records that no second opinion contributed.
Failed lenses: FEASIBILITY.

## Critical

None.

## Important

- [ ] Publication placements enum has no slot for /how-i-build, forcing the second source of truth the content model forbids — Draft/published flag, placements (homepage proof, Work catalog, case study), display order, maturity, public checked date, optional product/source/documentation URLs. — failure: Section 11 requires /how-i-build to render four responsibility sections, each naming a project and showing project-specific evidence (Agent Config, Agent Team, Control Center, Prism). /how-i-build currently references none of them, so the page must obtain those four records from somewhere. The placement enum is exactly three values, so an implementer building Milestone 4 cannot express "appears on How I build" in the record and will hardcode the four slugs, labels, and evidence captions in app/how-i-build/page.tsx. That directly violates the rule that Homepage, Work, related-project links, and detail pages consume the same record rather than maintaining separate status/summary arrays: when Control Center is later re-labeled from Prototype or its fixture is swapped, /work and the case study update and /how-i-build silently keeps the stale copy — the exact divergence the typed catalog exists to prevent. — fix: Add a fourth placement value (how-i-build / method) to the Publication field group, and give the Interaction field group an explicit compact variant (separate fixture ID plus display copy) so the four responsibility panels are derived from the same record instead of a hand-maintained list on the page. — reviewers: lens:DESIGN, lens:SCOPE.

- [ ] Related-project links are not gated on published state, so the draft flag cannot be used as rollback without creating 404s — Stable slug, name, category, summary, contribution statement, related slugs, optional detail route. A slug alone does not create a published route. — failure: Related slugs are stored per record and the Continue section renders one or two related projects. The only unpublish mechanism is the draft/published flag. Sequence: Vora ships in Milestone 2 with relatedSlugs including agent-team; Milestone 3 ships Agent Team; after publication the merge-policy claim fails review, so agent-team is flipped to draft to roll it back. /work/agent-team stops being generated, but /work/vora still renders its Continue link to /work/agent-team, and Control Center's Continue section does the same — a hard 404 from two published pages. A1 only checks that no draft route is published; it never checks that no published page links to a draft slug, so verification passes while the site is broken. — fix: State that related-slug resolution filters on published state at render time (dropping unresolvable slugs, and omitting the Continue section when none remain), and add that rule plus a build-time assertion that no published record links to an unpublished slug to A1. — reviewers: lens:DESIGN, lens:SECURITY.

- [ ] The Work page composition table deletes the existing client-work block and audit conversion path with no migration note — app/work/page.tsx:130 — failure: Section 4 gives an exhaustive ordered composition for /work: intro and filters, featured work, project list, How I build and notes, contact. The live page contains two sections with no slot in that list: the client-work paragraph explaining that client engagements are the newer half of the practice with its link to /results, and the closing "Start with the audit" block stating the $750 fixed-scope audit with its /tools/ai-readiness link. An implementer building section 4 literally removes both. The spec's scope carve-out names homepage hero, service content, packages, client previews, and blog infrastructure as out of scope, and section 2 forbids silently removing existing projects — neither protects these two blocks, and section 4's contact row replaces the audit CTA with "Discuss a project". Result: /work loses its only pricing/offer statement and the readiness-tool funnel, and the page's standing promise that a named client case study goes here is deleted. — fix: Either add explicit rows to the section 4 composition table preserving the client-work positioning block and the audit/readiness CTA (stating their new order relative to the catalog), or state in section 2 that removing them is an intended change and record where that content goes instead. — reviewers: lens:DESIGN.

- [ ] Filter state is specified as URL navigation and as in-page component state simultaneously; the focus and count-announcement requirements cannot hold — Filter navigation keeps focus on the selected control and exposes the resulting count without forcing focus into the list. — failure: The same paragraph mandates native links with ?category=products, server rendering of the selected category on direct load, unknown values resolving to All, and back/forward restoration — i.e. filter state is owned by the URL and applied by a navigation. A document navigation destroys the activated control and resets focus to the document (Next's App Router also moves focus to the new page root), so after clicking "Developer tools" focus is not on the selected control, and an aria-live count region mounted fresh by that navigation announces nothing because live regions only announce subsequent mutations. Satisfying focus retention and the count announcement requires client-side state with no navigation, which then breaks deep-load rendering and back/forward unless duplicated. An implementer will pick one half and fail the other half of A1/A4, and the chosen half is not stated. — fix: Pick one owner and write it down: URL-owned filtering via a client component that reads the category from the URL, updates it with a shallow history push (no remount), mirrors it into component state for focus retention, and renders the server-resolved category on first load — then restate the focus and announcement requirements in terms of that mechanism, or drop them in favor of plain navigation semantics. — reviewers: lens:DESIGN, lens:SCOPE.

- [ ] Responsive thresholds keyed to "available content width" are not expressible in the existing viewport-breakpoint system and mismatch at first paint — components/AIChat.tsx:45 — failure: The spec requires layout decisions to use available content width after the chat sidebar offset, stacking feature/workflow columns below 800px and project rows below 600px. The 360px reserve is applied by .chat-offset at min-width:768px and cancelled only by html[data-chat="collapsed"], an attribute written by a client effect after localStorage is read — it is absent from server HTML and from first paint. Two concrete breaks: (a) a returning visitor with chat collapsed on a 1000px window is served markup whose available width is 640px (stacked per the 800px rule) and then hydration widens the column to 1000px, so the feature stays stacked in a 1000px column or reflows visibly; (b) at a 768px viewport with chat open, available width is 408px — below the 600px stack rule — yet every existing row uses sm:grid-cols-12 (640px viewport), so twelve columns are crammed into 408px. The repo contains no container queries or container-type declarations, so no stated mechanism can evaluate these thresholds. — fix: Name the mechanism: declare a container on the .chat-offset wrapper and express the 800px/600px rules as container queries (or as paired viewport + html[data-chat] selectors for both open and collapsed states), and specify the server-render default for data-chat so first paint matches the hydrated offset. — reviewers: lens:DESIGN.

- [ ] Two incompatible status vocabularies are merged into one maturity field, demoting Vora relative to its unchanged neighbor — app/work/page.tsx:114 — failure: Today /work renders Vora, ChapterHQ, and the site assistant as a uniform set with the mono label "live" and a pulse dot; the homepage uses live / running / in development. The spec sets Vora's maturity to Implemented while stating that supporting projects retain evidence-backed existing labels until reviewed, and defines Implemented as meaning the capability exists in source, not that production availability was verified. After migration the catalog renders Vora as "Implemented" in the same row set where ChapterHQ still reads "live" and Real Estate Maite reads "in development" — three scales with no common ordering, and the flagship product reads as less shipped than the project beside it. This also contradicts the instruction not to relabel existing projects during catalog migration, since Vora's visible status changes. — fix: Define maturity as one closed enum covering every catalog and homepage entry, give the mapping from the current live/running/in-development labels into it (including whether Vora keeps a separate deployed/live signal independent of maturity), and state that the old status markers are removed everywhere in the same change rather than coexisting. — reviewers: lens:DESIGN.

- [ ] Vora failure branch doubles the state machine beyond the demonstration's stated purpose — A sample-outcome selector offers Succeeds and Fails, defaulting to Succeeds. — failure: The stated purpose of this demonstration is to show an AI employee proposing an action that a person can approve or reject. Approve and reject are fully covered by Request received, Awaiting approval, Approved, Completed, and Rejected. The Fails selector adds a sixth state, a pre-state control that must restart the walkthrough at Request received when changed mid-run, a branching rule where Show sample result routes to Completed or Execution failed based on prior selector state, a Reset that must restore both the step and the selector value, an extra fictional error fixture, an extra text-walkthrough branch, and a dedicated A2 assertion that failure never shows a delivery confirmation. Concrete break: a visitor selects Fails at Awaiting approval; the implementation must discard the pending approval state and rewind, a transition that exists only because the selector exists. Execution reliability is a different claim from approval gating and is asserted by no success criterion. — fix: Ship the approve/reject walkthrough only: Request received, Awaiting approval, Approved, Completed, Rejected, Reset. Delete the outcome selector, Execution failed state, mid-run restart rule, and the failed-execution A2 clause. Move the failure branch to the follow-on list alongside voice and industry switching. — reviewers: lens:SCOPE.

- [ ] Vora asset capture is specified against a real system with real supported actions, with no sandbox/test-credential requirement — Asset production must select a real supported action whose autonomy configuration requires approval. — failure: The spec correctly forbids the website demo from acting ("No website demonstration sends a message, approves a real action..."), but pushes the risk into asset production and never bounds that environment. It requires selecting and verifying a real supported action and its autonomy configuration, and permits "A fresh recording from an isolated demo tenant" without requiring that capture be done there, without requiring provider credentials in test/sandbox mode, and while acknowledging "Vora can auto-execute some destructive tools under its autonomy policy; approval is not universal." Concrete sequence: to verify the approval path, Caleb runs the follow-up action in the live production Vora instance against an existing contact record; the messaging/voice provider is configured with live credentials, so approving the example delivers a real SMS/email to a real customer, and a sibling tool in the same request auto-executes under the autonomy policy against production CRM data. The capture also becomes the source for a public poster, so any production customer record visible in the frame crosses the trust boundary at the same time. — fix: Make capture-environment isolation a hard precondition in §6 and the asset inventory: verification and recording occur only in a disposable tenant seeded with invented records, with messaging/voice/email providers on sandbox or test credentials and no live customer contacts reachable; record the tenant identifier and credential mode in the internal evidence notes; prohibit capture or action verification against the production tenant, and make "capture environment isolated, providers in test mode" an A5 line item. — reviewers: lens:SECURITY.

- [ ] Captured fixtures are required to preserve source identifiers verbatim, with no redaction rule or automated secret/path denylist — Preserve the exact event order of the version-pinned captured run or contract-derived fixture; visitor-facing labels may be shorter, but an optional technical view retains the source identifiers. — failure: §10 mandates fidelity to a captured local CLI run and requires the receipt to show "limits/usage where recorded, and cleanup fields", while §13 only says "Use descriptive filenames without private paths, customer IDs, tokens, or internal hostnames. Strip unnecessary image metadata" — a rule about filenames and images, not about fixture JSON bodies copied out of a real run. A5's "public records/fixtures contain no private identifiers" is an unaided manual eyeball check with no field allowlist, and it directly conflicts with the fidelity requirement, so the conflict resolves toward pasting the raw capture. Concrete sequence: a real `prism` run is captured on Caleb's machine; its receipt/trace contains workspace and cleanup paths such as `/Users/calebbolden/Projects/...`, temp/sandbox directory names, PIDs, the provider/model and org or key identifier in the usage record, and the local hostname. The fixture is committed to the public content module to keep the six events and identifiers exact, ships in the client bundle for `/work/prism`, and is then served to every visitor and indexed. The same gap applies to §8's compiler capture ("Generate the two sample output pairs offline from harmless fragments using the real compiler"), whose output can embed private fragment paths. — fix: Define a mandatory sanitization contract for any `captured` fixture: an explicit allowlist of publishable fields (event name, order, declared limits, plain-language cleanup status), redaction of paths, hostnames, PIDs, account/org/key identifiers, and env values, and a rule that provenance stays honest — a sanitized capture is labeled `captured (redacted)` rather than raw `captured`. Back it with an automated test in `npm run test` that fails on a denylist scan of all public records/fixtures (`/Users/`, `.local`, internal hostnames, `sk-`/token prefixes, IP literals), so this is not a manual-review-only gate. — reviewers: lens:SECURITY.

## Minor

- WorkDetail is named as the migration starting point but shares no contract with the new case-study layout — components/WorkDetail.tsx:17 — failure: WorkDetail's props are name, status ('live' | 'building' | 'running'), whatItIs, whoUsesIt, aiInside, techBands, stackLine, href, sheet, media, and it renders exactly those five fixed bands with no back link, demo frame, disclosures, or related projects. The new contract needs introduction with contribution and maturity, primary demonstration, problem and workflow, engineering decisions, credits, limits, About this example, and Continue — zero overlapping fields, and the status union literally cannot hold "Developer preview" or "Prototype". Because Milestone 1's completion condition is that existing project pages still work, the cheapest path is bolting optional props onto WorkDetail, producing one component that renders two mutually exclusive page shapes and carries both status and maturity. Three existing pages then block any cleanup of the old bands. — fix: Say explicitly that the new case-study layout is a new component, that WorkDetail is retained unchanged until ChapterHQ, the site assistant, and Open source are converted, and name the conversion of those three pages as a deliverable (or an accepted long-lived exception) rather than calling the old layout a starting point. — reviewers: lens:DESIGN.

- "Shared interaction behavior" is a linear-stepper contract that three of the five demonstrations do not fit — Manual Next/Previous controls are the default; no automatic playback is required for the first release. — failure: Only Prism and Agent Team are ordered sequences. Vora is a branching state machine whose table defines transitions by named buttons ("Review proposed action", "Approve example", "Show sample result") and lists no Previous transition at all; Agent Config is a variant toggle plus a stale/regenerate pair; Control Center is a filter-and-select panel with no steps. A verifier applying the shared contract and the acceptance line that Previous/Next disable at the ends will look for Previous/Next in Vora, Agent Config, and Control Center and find none, and cannot tell whether that is a defect or out of scope. Practically, Reset and the simulation label are the only genuinely shared behavior, so the per-case-study JS budget cannot assume a shared stepper component. — fix: Split the section into behavior that is universal (deterministic initial state, Reset, text equivalent of active state, no network effects, no persistence) and behavior that applies only to ordered-sequence demos (Next/Previous, end-disabling, Previous clears later state), and tag each of the five demos with which pattern it implements. — reviewers: lens:DESIGN.

- Analytics event contract is specified for a release that does not include analytics — If analytics already has a reviewed provider, measure only `project_open`, `demo_start`, `demo_complete`, `technical_detail_open`, and `contact_click`, with project slug and fixed scenario ID. — failure: The same paragraph states that adding an analytics service is a separate task and this release does not depend on it, which makes the whole block conditional on repository state the spec does not assert. The result is five named events, a payload schema, a transmission prohibition list, and a definition of completion that includes deliberately blocked scenarios, all of which an implementer must either build against an unconfirmed provider or skip. Concrete waste: an implementer wires demo_complete firing rules into the Agent Team security-block terminal state and the Vora rejected state to satisfy the completion definition, then discovers no reviewed provider exists and the instrumentation is dead code in the shipped bundle, counted against the 100KB demo budget. Measurement appears in none of the five success criteria and in no scope-table row. — fix: Delete the analytics paragraph from section 15. If instrumentation is wanted later, specify it in the separate analytics task where the provider question is actually answered. — reviewers: lens:SCOPE.
