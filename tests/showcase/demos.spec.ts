import { test } from './helpers';

test.skip('first real demo chunk blocked leaves static frame, walkthrough and recovery @release', () => {
  // P4: T04/T05 must replace this with a blocked request for their actual island chunk.
  // No real island exists in T03; readiness CSS is covered separately in access.spec.ts.
});
test.skip('all five real demos complete named transitions and reset @release', () => {
  // Each demo owner adds real transition checks; T12 runs them together.
});
